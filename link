# Bash only version of `readlink -f`
MINIMA=$(cd "$(echo "${DIR%/*}")" 2>/dev/null; pwd -P)

ln -s $MINIMA/rc/bashrc $HOME/.bashrc
ln -s $MINIMA/rc/gitconfig $HOME/.gitconfig
ln -s $MINIMA/rc/inputrc $HOME/.inputrc
ln -s $MINIMA/rc/screenrc $HOME/.screenrc
ln -s $MINIMA/rc/vimrc $HOME/.vimrc
